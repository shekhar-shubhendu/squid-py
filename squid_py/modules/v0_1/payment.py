def lockPayment(service_agreement_id, service_definition, *args, **kwargs):
    print('CALLED WITH {} {} {} {}'.format(service_agreement_id, service_definition,
                                           args, kwargs))
